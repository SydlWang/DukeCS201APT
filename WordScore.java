public class WordScore {
    public int[] score(String[] words, String want, String avoid) {
        // replace with your code
        //.tolowerCase and .toCharArray
        //use .indexOf and .contains to check if letter in want it in words then add to the score
        return null;
    }
}